apiVersion: v1
kind: Service
metadata:
  name: infrastructure-service
  namespace: infrastructure
spec:
  selector:
    app: infrastructure
  ports:
    - protocol: TCP
      port: 3005
      targetPort: 3005
  type: ClusterIP