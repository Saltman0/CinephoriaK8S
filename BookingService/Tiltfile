docker_build('saltman0/cinephoria-booking', '../../CinephoriaBookingService')

k8s_resource(
    new_name='booking',
    workload='booking-deployment',
    objects=['booking-doppler-secret', 'booking', 'booking-referencegrant', 'booking-doppler-token-secret'],
    labels=["backend"]
)