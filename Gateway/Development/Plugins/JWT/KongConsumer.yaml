apiVersion: configuration.konghq.com/v1
kind: KongConsumer
metadata:
  name: jwt-consumer
  namespace: gateway
  annotations:
    kubernetes.io/ingress.class: kong
username: jwt-user
credentials:
  - jwt-secret