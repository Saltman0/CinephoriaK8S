apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: jwt-doppler-secret
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: jwt-doppler-token-secret
  managedSecret:
    name: jwt-secret
    namespace: gateway
    type: Opaque