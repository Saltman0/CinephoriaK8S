apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kong-httproute-dashboard
  namespace: gateway
spec:
  parentRefs:
    - name: kong-gateway
      sectionName: https
    - name: kong-gateway
      sectionName: http
  hostnames:
    - "cinephoria.lol"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /dashboard
      backendRefs:
        - name: dashboard-service
          namespace: dashboard
          kind: Service
          port: 3006