apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-application-user
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Saltman0/CinephoriaK8S
    targetRevision: main
    path: UserService

  destination:
    server: https://kubernetes.default.svc
    namespace: user

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
