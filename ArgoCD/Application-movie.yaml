apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-application-movie
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Saltman0/CinephoriaK8S
    targetRevision: main
    path: MovieService

  destination:
    server: https://kubernetes.default.svc
    namespace: movie

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
