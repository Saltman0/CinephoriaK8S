apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-application-dashboard
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Saltman0/CinephoriaK8S
    targetRevision: master
    path: DashboardService

  destination:
    server: https://kubernetes.default.svc
    namespace: dashboard

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
