apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: dashboard-doppler-secret
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: dashboard-doppler-token-secret
    namespace: dashboard
  project: cinephoria-dashboard-service
  config: prd
  managedSecret:
    name: dashboard-secret
    namespace: dashboard
    type: Opaque